ALTER SESSION SET CONTAINER = TESTDB;
ALTER SESSION SET CURRENT_SCHEMA = test_user;

CREATE OR REPLACE PROCEDURE my_procedure (p_msg IN VARCHAR2) IS
BEGIN
  INSERT INTO proc_log (msg) VALUES (p_msg);
  COMMIT;
END;
/

CREATE TABLE proc_log (id NUMBER GENERATED BY DEFAULT AS IDENTITY, msg VARCHAR2(100));
COMMIT;

